export interface Booking {
  name: string;
  price: number;
  seats: string;
  id: string;
}

export interface Plane {
  image: string;
  name: string;
  description: string;
  price: number;
  seats: string;
}
